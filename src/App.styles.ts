import { css } from '@emotion/core'

export const globalStyles = css`
    html,
    body,
    div {
        margin: 0;
        padding: 0;

        box-sizing: border-box;
    }

    body {
        font-family: monospace;
    }
`
